"""案例自动检索与入库。

职责边界：本模块只负责「把候选案例送到人面前」，**不负责替人拍板**。
抓到的一律进 structured_candidates，任何情况下都不自动进 structured。

红线（继承 AGENTS.md，不可协商）：
  · 不绕过 robots.txt，不高频请求
  · 不抓需登录 / 验证码 / 非公开接口的数据
  · 不编造案例、金额、机关、法律依据
  · 不删除已有原始数据
"""
