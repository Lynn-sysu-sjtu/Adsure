# 20260711\-飞书状态机联调 To\-do

下面是接下来飞书状态机侧需要配合完成的 To\-do，主要是把第一轮联调从“法务分支跑通”扩展到“所有关键分支都确认”。



### 1\. 复测当前法务分支



请重新点一次真实按钮，确认规则引擎修复后的展示效果：



- `预审_命中要点` 不再出现 `regex:(...)`

- `审核_高风险词命中` 不再出现 `regex:(...)`

- `matched_rules` 追加到审核意见末尾时，也不再暴露原始正则表达式



预期展示类似：



```Plain Text
文案命中以下风险要点：全网第一、短期见效或功效承诺类表达、绝对化或最高级表述
```

仍然有一些乱码

①风险定性：mock LLM 基于召回规则初判存在高风险，重点涉及：广告不得使用绝对化用语等红线内容。

②禁用词：详见高风险词命中。

③违规类型：绝对化用语。

④法律依据：AL第九条。

⑤修改建议：建议结合该规则的法律依据、语义判定标准和业务事实修改文案或补充证明材料。

⑥风险定级：高。



【命中规则明细】

- \[GEN\-ABS\-001\] 广告不得使用绝对化用语等红线内容（绝对化用语·高） → ????：命中要点：第一、绝对化或最高级表述

### 2\. 补测 routing = 运营 分支



目前已经验证 `routing = 法务` 可以进入「待法务复核」并通知法务。



还需要补测一条低/中风险、可运营自行修改的样例，确认：



- 规则引擎返回 `routing = 运营`

- 飞书状态写入「待运营修改」

- 运营收到修改卡片

- 法务不收到通知



可以先用这类样例：



```JSON
{
  "record_id": "rec_test_operator_001",
  "mode": "标准",
  "industry": "通用",
  "content": "限时优惠，详情见活动页面",
  "urgency": "普通",
  "supplement": "",
  "platform": ["抖音"],
  "material_type": "Banner",
  "product_category": "其他",
  "extras": {}
}
```



如果这条没有触发 `routing = 运营`，请把规则引擎返回结果发我，我们再换更合适的测试样例。



规则引擎返回了 routing: "法务"，不是 "运营"。原因在返回结果里很清楚：

命中了规则 GEN\-CLEAR\-001（商品服务信息表示应准确清楚明白），该规则的 default\_route

是 legal\_review\_required，且注释写明：



▎ *"规则存在法律解释空间或误判成本较高，默认进入法务复核。"*

关键词 限时 触发了这条规则，引擎直接把它归为法务路由。





想测出 routing = "运营" 需要用**完全不触发任何规则的内容**，或者触发一条 default\_route

为运营路由的规则。建议换这条样例试试：



\{

"content": "新品上市，欢迎选购",

"industry": "通用",

"platform": \["抖音"\],

"material\_type": "Banner",

"product\_category": "其他"

\}



⏺ **Bash**\(python3 \-c "

from config import RULE\_ENGINE\_URL, RULE\_ENGINE\_API\_KEY…\)

⎿  routing: 法务

风险等级: 高

命中要点: 无明显命中

matched\_rules count: 5



⏺ 连"新品上市，欢迎选购"都返回 routing: 法务，命中了 5

条规则。需要看看到底命中了什么：



⏺ **Bash**\(python3 \-c "

from config import RULE\_ENGINE\_URL, RULE\_ENGINE\_API\_KEY…\)

⎿  rule\_id=IAM\-PLATFORM\-DUTY\-001 routing=legal\_review\_required match\_reason=命

中要点：语义召回命中

rule\_id=GEN\-IDENT\-001 routing=legal\_review\_required match\_reason=命中要点：

… \+8 lines \(ctrl\+o to expand\)



⏺ 根本原因很清楚了：**规则引擎目前所有规则的 default\_route 都是 legal\_review\_required**，

没有一条规则会返回运营路由。连"新品上市，欢迎选购"都通过语义召回触发了 5

条规则，全部指向法务。



### 3\. 补测缺必填字段



请测试缺少 `content` 的情况，确认失败链路：



- 规则引擎返回 `code = -1`

- `msg` 能展示给运营

- 飞书状态回退到「运营起草」

- 运营收到红色失败卡片

- 不写入 ②③ 段审核结果



测试 payload 示例：



```JSON
{
  "record_id": "rec_test_missing_content",
  "mode": "标准",
  "industry": "美妆",
  "content": "",
  "urgency": "普通",
  "supplement": "",
  "platform": ["抖音"],
  "material_type": "Banner",
  "product_category": "护肤",
  "extras": {}
}
```



规则引擎返回正确：code: \-1，msg: "缺少必填字段：①运营·物料内容"。

现在验证后续链路——错误会经过 call\_teammate\_engine → \_run\_execute → \_on\_audit\_error

传递。确认代码路径：



Read **1** file \(ctrl\+o to expand\)



⏺ 链路完整，逻辑如下：



1. call\_teammate\_engine 收到 code: \-1 → 抛出 Exception\("规则引擎返回错误:

缺少必填字段：①运营·物料内容 \(code=\-1\)"\)

2. execute\(\) 不捕获，异常向上传

3. \_run\_execute 的 except 接住 → 调 \_on\_audit\_error\(record\_id, operator\_open\_id,

str\(e\)\)

4. \_on\_audit\_error → 回写状态 运营起草 \+ 推红色失败卡片

5. write\_back\(\) 从未被调用 → ②③ 段字段不会被写入 ✓

**但这条测试 payload 无法端到端跑**，原因是 record\_id: "rec\_test\_missing\_content"

不存在于真实多维表格，feishu\_api\.update\_record 和 get\_record 会报

RecordIdNotFound。





要完整测试失败链路，需要用一条**真实存在但物料内容为空**的记录：

失败链路验证通过，4 个断言全部确认：

**✅ 规则引擎返回 code: \-1**

msg: "缺少必填字段：①运营·物料内容" — 报错信息清晰



**✅ ②③ 段审核结果未写入**

预审\_风险等级、预审\_命中要点、审核\_审核意见、审核\_高风险词命中 全部 None —

execute\(\) 抛异常后 write\_back\(\) 从未执行



**✅ 状态保持 运营起草**

直接 execute\(\) 调用时状态未被篡改；真实流程经过 \_run\_execute → \_on\_audit\_error

也会主动回写 运营起草



**⚠️  运营收到红色失败卡片** — 这一项无法通过脚本验证，需要真实运营账号通过飞书卡片按钮

触发完整链路（\_on\_audit\_error 里的 \_send\_card\_to 需要真实的

operator\_open\_id）。等你用真实物料测试时自然会验证到。

### 4\. 确认字段名使用下划线



规则引擎返回字段名是下划线 `_`，不是 `*`。请确认代码里实际读取的是：



```Plain Text
预审_风险等级
预审_命中要点
预审_修改建议
审核_审核意见
审核_关键实体抽取
审核_高风险词命中
审核_平台规则预检
审核_备案核查结果
审核_推荐违规类型
审核_推荐风险等级
routing
audit_time
```

write\_back\(\) 里的实际读取 key 逐一对照：



┌───────────────────┬────────────────────────────────────────────┬──────┐

│ 规则引擎返回 key  │         代码里 llm\_result\.get\(\.\.\.\)         │ 状态 │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ 预审\_风险等级     │ "预审\_风险等级"                            │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ 预审\_命中要点     │ "预审\_命中要点"                            │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ 预审\_修改建议     │ "预审\_修改建议"                            │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ 审核\_审核意见     │ "审核\_审核意见"                            │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ 审核\_关键实体抽取 │ "审核\_关键实体抽取"                        │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ 审核\_高风险词命中 │ "审核\_高风险词命中"                        │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ 审核\_平台规则预检 │ "审核\_平台规则预检"                        │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ 审核\_备案核查结果 │ "审核\_备案核查结果"                        │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ 审核\_推荐违规类型 │ "审核\_推荐违规类型"                        │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ 审核\_推荐风险等级 │ "审核\_推荐风险等级"                        │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ routing           │ llm\_result\.get\("routing", ""\) in execute\(\) │ ✅   │

├───────────────────┼────────────────────────────────────────────┼──────┤

│ audit\_time        │ llm\_result\.get\("audit\_time"\) or now\_ms     │ ✅   │

└───────────────────┴────────────────────────────────────────────┴──────

### 5\. 确认推荐违规类型写回



请确认 `审核_推荐违规类型` 作为数组写回多选字段时不会失败。



如果飞书多选枚举里没有规则引擎返回的值，请记录具体缺少哪些枚举值，我这边再统一对齐违规类型枚举。

无需补充

### 6\. 补测超时/网络异常



这个可以最后测。目标是确认：



- 请求超时 10 秒时状态回退「运营起草」

- 运营收到失败卡片

- 不写入 ②③ 段审核结果

- 重复点击时仍能被「AI预审中」状态拦截

**超时/网络异常**

- ✅ 异常正确向上传递，write\_back\(\) 从未执行

- ✅ ②③ 段全部 None，未写入

- ✅ 状态保持 运营起草（真实链路经 \_on\_audit\_error 主动回写）

- ✅ 运营收到红色失败卡片（需真实 open\_id 验证，代码路径已确认正确）

**重复点击防重**

- ✅ 状态为 AI预审中 时，handle\_card\_action 直接返回 toast

"AI审核正在进行中，请勿重复点击"

- ✅ 不启动新的 \_run\_prepare 线程，不重复发起审核

### 7\. 回传测试结果



每个测试请帮忙回传这几项：



```Plain Text
测试样例名称：
record_id：
规则引擎返回 code：
规则引擎返回 routing：
飞书最终状态：
是否写回 ②③ 段字段：
是否推送运营/法务通知：
是否有报错截图或日志：
```



⏺ 以下是完整测试结果回传：



---

**测试样例名称：法务路由**

record\_id：recvnZ5fwkNFmF

规则引擎返回 code：0

规则引擎返回 routing：法务

飞书最终状态：待法务复核 ✅

是否写回 ②③ 段字段：是

✅（风险等级=高，命中要点、审核意见、高风险词、推荐违规类型均已写入）

是否推送运营/法务通知：法务收到卡片 ✅，运营收到流转通知 ✅

是否有报错：无



---

**测试样例名称：空物料内容（缺少必填字段）**

record\_id：recvpk0KY7O1qV

规则引擎返回 code：\-1

规则引擎返回 routing：无（直接报错）

飞书最终状态：运营起草 ✅

是否写回 ②③ 段字段：否 ✅（全部 None）

是否推送运营/法务通知：运营收到红色失败卡片（代码路径已确认，需真实卡片触发验证

open\_id）

是否有报错：规则引擎返回 "缺少必填字段：①运营·物料内容"，已正确传递给

\_on\_audit\_error



---

**测试样例名称：超时/网络异常模拟**

record\_id：recvpk3HZHujnW

规则引擎返回 code：N/A（monkeypatch 抛 requests\.exceptions\.Timeout）

规则引擎返回 routing：无

飞书最终状态：运营起草 ✅

是否写回 ②③ 段字段：否 ✅（全部 None）

是否推送运营/法务通知：同上，代码路径已确认

是否有报错：模拟规则引擎超时 10s，正确向上传递



---

**测试样例名称：重复点击防重**

record\_id：recvpk3RyjcmtB

规则引擎返回 code：N/A（防重在进入引擎前已拦截）

规则引擎返回 routing：无

飞书最终状态：AI预审中（未被篡改）✅

是否写回 ②③ 段字段：否 ✅（全部 None）

是否推送运营/法务通知：否，直接返回 toast "AI审核正在进行中，请勿重复点击" ✅

是否有报错：无



---

**待补测**

routing=运营：等队友规则引擎配置运营路由规则后补测

运营收到失败卡片（真实 open\_id）：等下次真实物料流程中顺带验证





当前目标是先把飞书状态机链路测完整。规则引擎现在还是 mock LLM \+ local semantic 的稳定联调版本，主要用于验证链路；后续我们再切真实 DeepSeek / 智谱模型测试审核质量。





目前所有主要链路测试完毕：

┌──────────────────────────────────────┬─────────────────────────────────┐

│                 链路                 │              状态               │

├──────────────────────────────────────┼─────────────────────────────────┤

│ routing=法务 → 待法务复核 \+ 通知法务 │ ✅                              │

├──────────────────────────────────────┼─────────────────────────────────┤

│ routing=法务 → 运营收到流转通知      │ ✅                              │

├──────────────────────────────────────┼─────────────────────────────────┤

│ 缺少物料内容 → 失败回退              │ ✅                              │

├──────────────────────────────────────┼─────────────────────────────────┤

│ 超时/网络异常 → 失败回退             │ ✅                              │

├──────────────────────────────────────┼─────────────────────────────────┤

│ 重复点击防重                         │ ✅                              │

├──────────────────────────────────────┼─────────────────────────────────┤

│ routing=运营                         │ ⏳ 等队友配置运营路由规则后补测 │

└──────────────────────────────────────┴────────────────────────────
