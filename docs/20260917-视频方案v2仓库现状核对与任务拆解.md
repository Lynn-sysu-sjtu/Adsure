# 实施方案 V2 × 仓库现状核对报告与任务拆解

- 核对日期：2026-09-17
- 核对基准：《审心 Adsure-视频广告合规审核-实施方案 V2》（2026-08-17，8 页）
- 核对分支：`codex/ads-penalty-rag-pipeline`（工作区含大量未提交/未跟踪内容）
- 测试环境：按 Makefile 新建 `.venv-handoff`（`requirements-handoff.txt`），`make test-handoff` 同口径

## 0. 总体结论

方案中**阶段 1（取证层）已全部落地并有加强**，**阶段 3 的 L2/L4 与阶段 4 的涵摄/报告/Web 回看也已超出文档进度**；
但两条主线完全空白：**阶段 2 IP 识别零代码**、**金标准集 `datasets/golden` 不存在**（方案第十四节自己点名的头号前置仍未动）。
另外整个视频证据层代码**至今未提交 git**，是当前最大的工程风险。

测试：backend/tests **368 passed / 3 failed**；3 个失败全在 crawler 幻觉丢弃逻辑，与视频方案无关，但在当前 HEAD 上就是红的。


## 0.5 Sprint A 执行进展（2026-09-17 晚更新）

已按本报告第三节启动并落库（分支 `codex/ads-penalty-rag-pipeline`）：

| 任务 | 状态 | 落点 |
|---|---|---|
| A1 取证层代码入库（5 commits） | ✅ | `0d32c16` 取证层 → `787f6f1` 找法层 → `2df6715` 用法层/脚本 → `59d51c9` 151 项测试；crawler WIP 未混入 |
| A3 金标准框架 | ✅ | `datasets/golden/`：规范+标注手册 v0.1+校验器+候选清单（实有唯一素材仅 4 条，采集是人工任务） |
| A4 阶段 2 IP 骨架 | ✅（骨架） | `d51e4d6`：18 主体起始库、向量检索契约、双层融合（VLM-only 模型级强制 pending）、结构化 prompt、19 项测试。**SSCD 真实 embedder、参考图、云 VLM provider 仍待做** |
| A6 L3 需资质词库 | ✅ | `e884e72`：17 条 + required_materials 贯通 LLM payload，5 项测试（差距 G10 消除主体） |
| A7 运行手册 | ✅ | `docs/20260917-视频取证层本地运行手册.md`；`run_review.py --demo --llm mock` 实测通过（5 风险+1 提示，含 needs_facts） |
| A2 crawler 红测 | ⏸ 未动 | 仍为 3 failed，属 crawler 工作流；负责人修复后 `make test-handoff` 应全绿 |
| A5 云 ASR 移植 + 字级时间戳验收 | ✅ | `VolcSeedASR` 入 providers（每次响应强制复验 words，缺失即 `WordTimestampMissing`）；验收脚本 verify/measure 两模式，已对 2026-09-17 真实留存样本 verify PASS（另发现同一音轨一次 no_speech，待金标准量化）；人工标签误差测量待标注 |

测试基线：**410 passed / 3 failed**（IP 19 + L3 5 + 火山 ASR 18；3 个失败仍全在 crawler）。
差距状态：G1 关闭；G3 由「零代码」降为「骨架完成、待模型/参考图/云 VLM」；G10 关闭；G4 部分缓解（云 ASR 已入新架构，云 OCR/VLM 仍缺）。


## 0.6 Sprint A+ 执行进展（2026-09-19 更新：手册承诺全链路收口）

继 §0.5 之后，围绕「让产品手册描述的视频审核流程真实成立」完成载体层与数据层（13 commits）：

| 任务 | 状态 | 落点与验证 |
|---|---|---|
| 环境修复 | ✅ | worktree 重建 `.venv-handoff`（389→448 passed）；readline 段错误根因定位（anaconda lib-dynload 的 .so）+ sitecustomize 修复，写进运行手册 `d73466c`；jobs 数据同步 19 个（source+evidence+asr+report+manifest，frames 省略） |
| A4+ 异步任务 API（手册 Step1–3 载体） | ✅ | `37f0d8b`：`backend/app/api.py` 提交-轮询-结果三段式，复用飞书 v0.2 契约（jsonschema 校验通过）；进度回调写 job.json；record_id 幂等；**真实端到端验证**：5s 保健食品视频 → completed（高 2 条），audit-response 与 report 接口可用；成本红线行为验证（16.4s 素材 29 帧>20 → failed 不静默）；9 项测试 |
| A4+ 云 VLM provider（手册 4.3 兜底） | ✅ | `6ed0528`：`providers/cloud_vlm.py` OpenAI 兼容协议（火山方舟实测通过）；temperature=0；外发审计（帧/prompt/response sha256）；key 泄露 REDACTED；**真实云验证**：简笔画米奇 → disney_mickey conf=0.99 且强制 pending（防幻觉生效）；9 项测试 |
| A4+ 云 OCR provider（手册 Step2） | ✅ | `06f2c5b`：`providers/cloud_ocr.py` 复用视觉模型做 OCR（比方案原计划省一套凭据）；**真实云验证**：大字误差 <1%、贴边小字 near_edge=True（L4 输入直接可用）；8 项测试 |
| A4+ SSCD embedder（手册 4.3「相似度 0.91」） | ✅ | `cfc6b14`：`app/ip/sscd.py` 官方 TorchScript（MIT，94MB）；CPU 单帧 49ms（目标 <200ms）；**关键 spike 结论**：SSCD 对小目标不敏感（15% 占比相似度仅 0.08），必须 bbox 裁剪后比对（恢复到 0.86+）→ 与 VLM 形成「找位置→精排」互补链路；7 项测试锁定该结论 |
| A6+ 裁决回流（手册 Step6） | ✅ | `04c8959`：`app/feedback.py` 七种裁决动作 → lexicon/case/ip 三个 append-only JSONL；`POST /api/jobs/{id}/adjudication` 定位字段从 report.json 读取防传错；词库校准建议聚合（不自动生效，人工 review 后应用）；5 项测试 |

**测试基线：448 passed / 3 failed**（3 个失败仍全在 crawler；新增 API 14 + 云 VLM 9 + 云 OCR 8 + SSCD 7 + 裁决回流 5）。

### 手册承诺对照（2026-09-19 快照）

| 手册步骤 | 状态 |
|---|---|
| Step1–3 飞书入口/卡片/异步 | 🟡 API 全就绪（含进度回调、成本红线、幂等）；**飞书机器人接线待外部凭据**（飞书应用 app_id/secret + 多维表格字段映射） |
| Step2 画面 OCR | ✅ 云（视觉模型路线）+ 本地（RapidOCR）双 provider |
| Step3 90s 出结果 / 成本红线 | ✅ API 层完成并实测（红线触发不静默） |
| Step4 查看逐秒证据 | ✅ report / report-page / media 三接口 + 深链 `?finding=N&t=秒` |
| Step5 六段式报告 | ✅ |
| Step6 裁决沉淀 | ✅ 三库分流 + 词库建议聚合（不自动生效） |
| 4.3 IP 双层校验 | ✅ 底库（SSCD）→ VLM 兜底 → 疑似待确认全链路；**差真实参考图入库**（收集是人工任务，每 IP 5–20 张） |

### 差距状态更新（对照 §2 清单）

- **G3 关闭**（IP 识别从「零代码」→ 全链路可跑，差参考图数据）
- **G4 关闭**（云 ASR/OCR/VLM 三路全部接入新架构，均有真实云验证）
- **G11 部分缓解**（飞书契约 API 就绪，机器人接线属外部配置）
- **G12 关闭**（异步任务 API 完成，FastAPI 提交-轮询模式）
- 新增待办：IP 参考图收集（人工）、飞书机器人接线（外部凭据）、金标准标注（人工，40–60 条）

## 1. 与方案逐项核对

### 1.1 阶段 1 · 取证层 — 基本全部达成，且有超出

| 方案要求 | 仓库落点 | 状态 |
| --- | --- | --- |
| ffmpeg 抽帧 + PySceneDetect（关键帧 ∪ 2fps） | `backend/app/pipeline/extract.py`、`frames.py` | ✅ |
| pHash 去重 + span 回填（防 L4 时长失效） | `extract.py`（`dedup_ratio`、回填日志） | ✅ |
| EvidenceUnit 带 t_start/t_end/bbox/frame_ids | `app/pipeline/evidence.py`（347 行） | ✅ |
| 字符 offset → 时间戳换算 | `evidence.py TranscriptIndex` | ✅ 有专项测试锁定 |
| ASR 拼接匹配、跨句违禁词不漏 | TranscriptIndex 跨句拼接（方案坑 #1 修复有测试：「本品是国家」+「级配方」） | ✅ |
| OCR 不拼接；merge.py 文本相似度 + bbox IoU 双条件 | `app/pipeline/merge.py`（阈值在 config） | ✅ |
| Provider 抽象 + 能力闸门（字级时间戳/bbox 硬指标） | `providers/base.py` `ASRCapabilities.word_timestamps`、`OCRCapabilities.bbox` | ✅ |
| 成本闸门基类、超限抛 `CostLimitExceeded` | `providers/base.py` 模板方法 | ✅ |
| 成本红线 ≤20 OCR / ≤4 VLM | `app/config.py`（OCR 按时长等比放宽，注释说明了理由） | ✅ |
| 去重削减率 <70% 告警不放宽上限 | `extract.py:336` | ✅ |
| Mock Provider，无 key 端到端可跑 | `providers/mock.py`（ASR/OCR/VLM 三个 mock） | ✅ |
| AC 自动机 offset 口径开机自检 | matcher 测试 `test_automaton_reports_character_indexes` | ✅ |
| 各云 Provider 实现 | **无任何真实云 ASR/OCR/VLM 实现类**，config 枚举了 aliyun/tencent/volcengine 但 providers/ 下只有 `mock.py`、`local.py` | ❌ 见 G4 |
| 超出方案：本地推理提前落地 | `providers/local.py`：faster-whisper + RapidOCR（CPU 可跑），`review_video.py` 真实视频端到端无需云账号 | ➕ 提前于方案路线图 |

### 1.2 阶段 2 · IP 识别 — 完全未开始（0 代码）

- 无 `ip_lib`/高危 IP 实体库；无 SSCD embedding；无 faiss；无 VLM 结构化 IP 巡检 prompt；无双轨交叉验证；无授权材料清单输出。
- 仅有骨架：`evidence.py` 里有 `VisualEntityEvidence` 类型、`MockVLMProvider` 有 ScriptedEntity 注入点；config 有 VLM provider 开关。
- 旧 MVP（`src/video_mvp/pipeline.py`）自己的报告里把「IP 形象权利库比对」明确列在 `not_connected` 清单中，口径一致：确实没做。
- 方案要求的 30–50 权利主体高危清单（法务输入）无任何落地产物。

### 1.3 阶段 3 · 找法层 — L1 达成，L2/L4 超预期但均未经法务复核，L3 缺失

| 项 | 现状 |
| --- | --- |
| L1 绝对化用语 49 条 + AC + 白名单 | ✅ `rules/lexicon/L1_absolute.yaml` 正好 49 条，带法条原文/豁免线索；坑 #2（裸词「第一」）有测试 |
| L2 行业禁用语（从处罚决定书反挖、挂真实案号） | 🟡 `L2_industry.yaml` 25 条，由 `scripts/mine_l2_terms.py` 生成、每条挂真实案号；**reviewed_by_legal=false**，覆盖以保健食品为主 |
| L3 需资质词 → 材料补充清单 | ❌ 无 L3 词库文件（schema 注释声称支持 L1/L2/L3）；旧 MVP `src/video_mvp/materials.py` 有材料 OCR 核验逻辑可复用；IP 授权材料清单随阶段 2 一起缺 |
| L4 必备要素 + 显著性核查 | 🟡 `mandatory.py`（422 行）+ `L4_mandatory.yaml` 5 个场景（保健食品免责语/医疗器械/OTC/广告可识别性/投资理财风险提示），时长/字号占比/贴边三个阈值齐全，带「非违法认定」措辞约束；**reviewed_by_legal=false**；化妆品、游戏等赛道未覆盖 |
| `recall.py` 对接凌淇现有规则引擎 | ❌ 文件不存在；config 有 `adsure_rule_engine_url` 与「留空降级本地词库」开关，但没有任何 HTTP client |
| 对接王亦澄类案库检索接口 | 🟡 `reasoning/cases.py` 只读本地 `data/structured[_candidates]` JSON，遵守了「approved 与候选分开、source_url 保留、仅供参照」三条纪律；`adsure_case_lib_url` 无远程 client |

### 1.4 阶段 4 · 用法层 + 报告 + Web — 主体已完成，飞书与异步服务未合入新架构

| 项 | 现状 |
| --- | --- |
| 三限定 prompt、三态输出（违规/需事实核验/不适用） | ✅ `reasoning/schema.py` Verdict：violation / needs_facts / not_applicable；`subsume.py` |
| 真实 LLM | ✅ `llm_deepseek.py`（DeepSeek，env 注入 key）；mock 可跑 |
| 六段式报告 + 免责声明 | ✅ `reasoning/report.py`（DISCLAIMER、finding 挂 t_start/bbox/duration） |
| Web 详情页：播放器 + 时间轴打点 + bbox 取证框 + 点击跳转 | ✅ `frontend/review.html`（单文件，支持载入报告 JSON + 本地视频、时间轴点击、键盘、框绘制；已纳入 git） |
| 飞书多维表格字段 + 机器人卡片 + 跳链（复用赵楚齐代码） | ❌ backend 内零飞书代码，仅 config 占位字段；旧 MVP 侧有 `tests/test_video_feishu_adapter.py`、`docs/视频报告转飞书v0.2轮询接口.md`，**尚未合入取证层新架构** |
| FastAPI + Celery + Redis 异步任务/进度回调 | ❌ backend 无 FastAPI app、无 celery；旧 MVP `src/video_mvp/api.py` 可参考（提交-轮询模式已跑通） |

### 1.5 方案第十四节「需要你推动的四件事」状态

| 事项 | 状态 |
| --- | --- |
| ① 40–60 条真实视频金标准（≥15 含知名 IP，双人交叉标注） | ❌ `datasets/golden` 不存在。`data/evaluation/video_claim_regression.json` 33 条文案级用例自述 `engineering_regression_not_legal_gold`；`data/video_mvp/jobs` 有 19 个工程跑批 job，非标注金标准。仅有合成替代品：`make_truth_ad.py` + `score_truth.py`（答案预埋的合成广告） |
| ② 法务敲定高危 IP 清单 | ❌ 未启动，无讨论底稿产物 |
| ③ 云 ASR/OCR/VLM 账号 | 🟡 旧 MVP 侧火山已联调：Seed-ASR 录音文件接口 2026-09-17 用 16.44s 美妆音轨实测通过（`docs/火山引擎模型接入.md`），解析层支持 words 字级时间戳但是否实带字级字段尚需按方案要求验收；豆包 VLM（≤12 代表帧）已接入旧 MVP。**新 backend 一侧三个云 provider 均无实现**；云 OCR 全仓库无真实接入（旧 MVP 只用 Apple Vision 本地 OCR） |
| ④ 凌淇/王亦澄接口 | ❌ 未对接，均为本地降级状态 |

### 1.6 方案第十一节验收指标可测性

金标准缺失导致全部硬指标（时间戳误差 ≤0.5s、花字召回 ≥85%、违规词召回 ≥95%、误报 ≤15%、L4 ≥90%、IP 召回 ≥90%/误报 ≤10%、端到端 ≤90s）**目前均无法测量**。
`score_truth.py` 只能在合成广告上给工程信号，方案明确说过合成数据替代不了真实准召率。

## 2. 差距清单（按优先级）

| # | 差距 | 严重度 | 阻塞什么 |
|---|---|---|---|
| G1 | 取证层全部代码与 371 个测试**未提交 git**（`backend/app/{pipeline,rules,reasoning}`、`config.py`、`backend/tests/` 全是 untracked） | 🔴 高 | 一切协作/交接，机器一丢全完 |
| G2 | 金标准集不存在（40–60 真实视频、≥15 IP、双人标注） | 🔴 高 | §11 全部验收指标、答辩可信度 |
| G3 | 阶段 2 IP 识别零实现（库/SSCD/faiss/VLM 巡检/融合/材料清单） | 🔴 高 | 首发范围两大风险类型之一 |
| G4 | 新 backend 无真实云 ASR/OCR/VLM provider；云 OCR 全仓未接 | 🔴 高 | 无 GPU 路线的落地 |
| G5 | 高危 IP 清单法务输入未启动 | 🟠 中 | G3 的前置 |
| G6 | ASR 字级时间戳端到端实测验收未做（方案：不满足立刻换供应商） | 🟠 中 | 全部时间定位可信度 |
| G7 | L1/L2/L4 三库均 reviewed_by_legal=false | 🟠 中 | 对外输出 |
| G8 | `recall.py` 凌淇规则引擎适配不存在 | 🟠 中 | 「找法复用」叙事闭环 |
| G9 | 王亦澄类案库远程接口未对接（只读本地 JSON） | 🟠 中 | 类案时效与覆盖 |
| G10 | L3 需资质词库与材料清单缺失 | 🟠 中 | 「要材料」处置路径（IP 也复用它） |
| G11 | 飞书入口未合入新架构（backend 零飞书代码） | 🟡 低 | 首发载体闭环（旧 MVP 侧可复用） |
| G12 | backend 无异步任务 API（FastAPI/Celery/进度回调） | 🟡 低 | 90s 任务的工程承载 |
| G13 | L4 仅 5 场景、L2 仅 25 词，参数全是起始值未标定 | 🟡 低 | 覆盖率/准确率 |
| G14 | crawler 3 个红测（HEAD 即红，与视频无关）；工作区 crawler WIP 与视频工作混在同一分支 | 🟡 低 | 主干健康度 |
| G15 | 方案文档过时：§13 仍写「31 测试/8 模块」，实际 371 测试；local provider、merge/interop、L2/L4、reasoning、review.html、火山联调均未反映 | 🟡 低 | 团队认知同步 |

## 3. 下一步任务拆解

### Sprint A · 本周可并行（不依赖法务、云账号、他人接口）

| ID | 任务 | 产出与验收 | 建议负责人 |
|---|---|---|---|
| A1 | **立即把取证层代码分 3–4 个 commit 提交**（pipeline+providers / rules / reasoning+frontend / tests），crawler WIP 不要混提 | `git ls-files backend/app/pipeline` 非空；干净 clone 后 `.venv-handoff` 跑测试通过 | 工程 |
| A2 | 修或显式 skip/登记 crawler 3 个红测（幻觉丢弃：`test_fabricated_quote_causes_field_to_be_dropped` 等） | `make test-handoff` 全绿 | crawler 负责人 |
| A3 | 金标准集工程框架：建 `datasets/golden/` 目录规范（视频、双人标注 YAML、分歧留档）、标注手册 v0.1；先清点 4 个根目录测试视频 + 19 个 job 能否入选 | 评审通过标注模板；≥10 条素材完成初筛入库 | 数据/运营 |
| A4 | **阶段 2 骨架（不依赖法务清单也能写代码）**：① IP 库 YAML schema 与加载器，字段严格对齐方案第五节（`ip_id/name_cn/rights_holder/library_type/law_ref/required_materials/reference_images/risk`），`library_type` 现在就留；② SSCD+faiss CPU spike，单帧 <200ms；③ VLM 结构化 prompt（强制「无/不确定」选项）；④ 双层融合：底库中→正式清单，仅 VLM 中→强制「疑似待确认」（方案坑 #6，基类强制置位+测试） | 用 2–3 个公开 IP 玩偶拍摄小样跑通；红队测试普通卡通形象不得进正式清单 | 工程（取证层原作者） |
| A5 | 把火山 Seed-ASR 适配进 `backend/app/providers/`，接能力闸门；写字级时间戳验收脚本（随机 10 段词核对真实秒数，误差 ≤0.5s），**不达标即拒绝启动并换供应商** | 验收报告落 `data/reports/`；provider 初始化闸门测试 | 工程 |
| A6 | L3 词库 v0.1：从现有类案库挖需资质表述（专利/认证/背书/医疗机构等），每条挂 required_materials 模板；IP 命中复用此处置路径 | `L3_qualifications.yaml` + 测试 | 工程+法务助理 |
| A7 | 补一份本地/云 provider 选择与密钥注入的运行手册（`.env.video.volc.example` 已有，补 backend 侧 `.env.example`——该文件目前未跟踪） | 新人按手册 30 分钟跑通 `review_video.py` | 工程 |

### Sprint B · 依赖法务 / 云账号 / 队友（可与 A 并行推进沟通）

| ID | 任务 | 前置 | 验收 |
|---|---|---|---|
| B1 | 法务复核 L1/L2/L4，翻转 `reviewed_by_legal`；存疑词条下线而非软化 | 法务排期 | 三库 version 升 reviewed |
| B2 | 用方案第五节起始清单开 IP 清单评审会，产出 30–50 主体清单 + 每主体 required_materials | 法务 | 清单 YAML 入库（reviewed） |
| B3 | 开通云 OCR（先选 1 家），实现 provider；用真实广告花字做 ≥85% 召回实测；不达标走多 provider 投票或本地微调预案 | 云账号 | 实测报告；OCR 调用 ≤20 红线保持 |
| B4 | 云 VLM provider（豆包/Qwen）接入新架构，执行 A4 的巡检 prompt | 云账号 | mock→真云切换只改 env |
| B5 | `recall.py`：凌淇规则引擎 HTTP 适配 + 本地词库降级；先按 `docs/案例库接入接口确认稿.md` 定契约 | 凌淇接口 | 契约测试；URL 为空时降级有测试 |
| B6 | 王亦澄类案库远程检索 client（`adsure_case_lib_url`），保持 approved/候选分离 | 王亦澄接口 | 远程不可用时回退本地 JSON |

### Sprint C · 金标准到位后的验收冲刺

| ID | 任务 | 验收 |
|---|---|---|
| C1 | 完成 40–60 条真实视频采集标注（违规文案类从市监局处罚公示反查；≥15 条含知名 IP；2 人交叉标注、分歧留档） | A3 框架填满；标注一致性报告 |
| C2 | 跑方案 §11 全部硬指标；不达标的参数重新标定并**回写 config 注明依据**（当前 config 自述全是起始值） | 指标报告入 `data/reports/` |
| C3 | backend 异步任务 API（提交/轮询/进度回调，可直接搬旧 MVP api.py 模式） | 30s 视频端到端 ≤90s |
| C4 | 飞书适配合入新架构：多维表格字段、互动卡片、Web 跳链（复用赵楚齐旧代码 + `docs/视频报告转飞书v0.2轮询接口.md`） | 在飞书内完成提交→卡片→回看→裁决全链路 |
| C5 | 裁决回流（规则校准/类案/IP 底库）字段与写入口预留 | 裁决动作能落库 |
| C6 | 更新方案文档至 v2.1：修正 §13 测试数与各阶段实际状态，记录对方案的偏离（本地 provider 提前、OCR 预算按时长放宽等） | 文档入库 |

## 4. 关键依赖关系

```text
A1(提交代码) ── 一切协作的前置
A3(金标准框架) ──→ C1(标注) ──→ C2(指标) ──→ 答辩
B2(IP清单法务) ──→ A4(IP骨架) 填充为正式库 ──→ IP准召指标
A5/B3(云ASR/OCR验收) ──→ C2 的时间戳/花字指标
B5/B6(队友接口) 与主线解耦：降级路径已在 config 留好，拿不到不阻塞
```

## 5. 附：核对方法与环境备注

- 测试命令：`.venv-handoff/bin/python -m pytest backend/tests -q`（本次新建该 venv，此前仓库不存在 Makefile 指定的 `.venv-handoff`）。
- 本机 anaconda python 直接跑 pytest 会在 readline 扩展段错误（环境问题，非项目问题）；应急可在 PYTHONPATH 放空 readline stub，但不建议写进项目。
- `backend/requirements-local-video.txt`（faster-whisper/rapidocr）未计入 handoff 环境，跑 `review_video.py` 需另装。
- 视频相关测试文件 8 个共 151 项（evidence/matcher/merge/mandatory/providers/interop/reasoning/local_providers），方案「31 项」为 8 月中旬口径。
